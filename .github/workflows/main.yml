name: first-workflow
on: [workflow_dispatch]
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: say-hello
        run: echo "Hello!"
      - name: say-good-bye
        run: echo "Good Bye!"
